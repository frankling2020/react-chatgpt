upstream backend {
    server backend:8000;
}


server {
    listen 80;
    server_name localhost;

    location / {
        root /app/build;
    }

    location /submit {
        proxy_pass http://backend;
    }
}